var keyboard = new KeyboardState();
var stage;
var megaman;
var props = [];

function setup() {
	createCanvas(600, 420);
	stage = new Stage();
}

function draw() {
	stage.show();
}

function keyPressed() {
	// if (keyCode === LEFT_ARROW) {
	// 	left();
	// }
}